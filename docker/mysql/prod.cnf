[mysqld]
# Tuned for a small (2-4 GB RAM) server.

performance_schema=OFF

innodb_buffer_pool_size=512M
innodb_flush_log_at_trx_commit=1
max_connections=100
skip-name-resolve

slow_query_log=1
slow_query_log_file=/var/lib/mysql/slow.log
long_query_time=1
