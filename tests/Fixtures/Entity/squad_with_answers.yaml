App\Classic\Entity\TournamentSessionTeamAnswer:
    answer_existing_{1..5}:
        tournamentSessionTeam: '@session_team_existing'
        questionNumber: '<current()>'
        isCorrect: true
    answer_existing_no_{1..3}:
        tournamentSessionTeam: '@session_team_existing'
        questionNumber: '<($current + 5)>'
        isCorrect: false
