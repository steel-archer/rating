{% extends 'base.html.twig' %}

{% block title %}{{ 'moderator.player_claims'|trans }} — {{ 'site.title'|trans }}{% endblock %}

{% block breadcrumbs %}
    <nav class="breadcrumbs"><a href="{{ path('home') }}">{{ 'nav.home'|trans }}</a><span>{{ 'moderator.player_claims'|trans }}</span></nav>
{% endblock %}

{% block body %}
    <h1>{{ 'moderator.player_claims'|trans }}</h1>

    {% for message in app.flashes('error') %}
        <div class="flash-error">{{ message }}</div>
    {% endfor %}

    {% if claims is empty %}
        <p>{{ 'moderator.no_pending_claims'|trans }}</p>
    {% else %}
        <table aria-label="{{ 'moderator.player_claims'|trans }}">
            <thead>
                <tr>
                    <th>{{ 'moderator.player_claim_user'|trans }}</th>
                    <th>{{ 'moderator.player_claim_type'|trans }}</th>
                    <th>{{ 'moderator.player_claim_details'|trans }}</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                {% for claim in claims %}
                    <tr>
                        <td>{{ claim.user.email }}</td>
                        <td>
                            {% if claim.new %}
                                {{ 'moderator.player_claim_new'|trans }}
                            {% else %}
                                {{ 'moderator.player_claim_existing'|trans }}
                            {% endif %}
                        </td>
                        <td>
                            {% if claim.new %}
                                {{ claim.lastName }} {{ claim.firstName ?? '' }} {{ claim.patronymic ?? '' }}
                                {% if claim.town %} ({{ claim.town.name }}){% endif %}
                            {% else %}
                                <a href="{{ path('player_show', {id: claim.player.id}) }}">{{ claim.player.fullName }}</a>
                            {% endif %}
                        </td>
                        <td class="claim-actions">
                            <form method="post" action="{{ path('moderator_player_claim_approve', {id: claim.id}) }}">
                                <input type="hidden" name="_token" value="{{ csrf_token('player_claim_' ~ claim.id) }}">
                                <button type="submit" class="btn btn-approve">{{ 'moderator.approve'|trans }}</button>
                            </form>
                            <form method="post" action="{{ path('moderator_player_claim_reject', {id: claim.id}) }}">
                                <input type="hidden" name="_token" value="{{ csrf_token('player_claim_' ~ claim.id) }}">
                                <button type="submit" class="btn btn-reject">{{ 'moderator.reject'|trans }}</button>
                            </form>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    {% endif %}
{% endblock %}
