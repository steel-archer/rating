{% extends 'base.html.twig' %}

{% block title %}{{ 'claim.submitted_title'|trans }} — {{ 'site.title'|trans }}{% endblock %}

{% block body %}
    <div class="card">
        <h1>{{ 'claim.submitted_title'|trans }}</h1>
        <p>{{ 'claim.submitted_description'|trans }}</p>
        <a href="{{ path('home') }}">{{ 'common.back_home'|trans }}</a>
    </div>
{% endblock %}
