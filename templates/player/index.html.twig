{% extends 'base.html.twig' %}

{% block title %}{{ 'nav.players'|trans }} — {{ 'site.title'|trans }}{% endblock %}

{% block breadcrumbs %}
    <nav class="breadcrumbs"><a href="{{ path('home') }}">{{ 'nav.home'|trans }}</a><span>{{ 'nav.players'|trans }}</span></nav>
{% endblock %}

{% block body %}
    <h1>{{ 'nav.players'|trans }}</h1>

    <turbo-frame id="players-list" data-sync-url src="{{ path('player_list') }}{{ app.request.queryString ? '?' ~ app.request.queryString : '' }}">
        <p>{{ 'common.loading'|trans }}</p>
    </turbo-frame>
{% endblock %}
