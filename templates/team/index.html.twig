{% extends 'base.html.twig' %}

{% block title %}{{ 'nav.teams'|trans }} — {{ 'site.title'|trans }}{% endblock %}

{% block breadcrumbs %}
    <nav class="breadcrumbs"><a href="{{ path('home') }}">{{ 'nav.home'|trans }}</a><span>{{ 'nav.teams'|trans }}</span></nav>
{% endblock %}

{% block body %}
    <h1>{{ 'nav.teams'|trans }}</h1>

    <turbo-frame id="teams-list" src="{{ path('team_list') }}">
        <p>{{ 'common.loading'|trans }}</p>
    </turbo-frame>
{% endblock %}
