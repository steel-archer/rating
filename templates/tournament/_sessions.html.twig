<turbo-frame id="tournament-sessions">
    <div class="card card-wide">
        <table>
            <thead>
                <tr>
                    <th>Майданчик</th>
                    <th>Місто</th>
                    <th>Дата</th>
                    <th>Представник</th>
                    <th>Ведучий</th>
                    <th>Команд</th>
                </tr>
            </thead>
            <tbody>
                {% for s in sessions %}
                    <tr class="{{ loop.index is odd ? 'row-light' : 'row-dark' }}">
                        <td><a href="{{ path('venue_show', {id: s.venue.id}) }}" data-turbo-frame="_top">{{ s.venue.name }}</a></td>
                        <td>{{ s.venue.town.name }}</td>
                        <td>{{ s.playedAt ? s.playedAt|date('d.m.Y') : '—' }}</td>
                        <td><a href="{{ path('player_show', {id: s.representative.id}) }}" data-turbo-frame="_top">{{ s.representative.fullName }}</a></td>
                        <td>
                            {% if s.host %}
                                <a href="{{ path('player_show', {id: s.host.id}) }}" data-turbo-frame="_top">{{ s.host.fullName }}</a>
                            {% else %}
                                —
                            {% endif %}
                        </td>
                        <td>{{ teamCounts[s.id] ?? 0 }}</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>

        {% include '_pagination.html.twig' with {paginationRoute: 'tournament_sessions_list', paginationParams: {id: tournament.id}} %}
    </div>
</turbo-frame>
