{% extends 'base.html.twig' %}

{% block title %}{{ 'nav.venues'|trans }} — {{ 'site.title'|trans }}{% endblock %}

{% block breadcrumbs %}
    <nav class="breadcrumbs"><a href="{{ path('home') }}">{{ 'nav.home'|trans }}</a><span>{{ 'nav.venues'|trans }}</span></nav>
{% endblock %}

{% block body %}
    <h1>{{ 'nav.venues'|trans }}</h1>

    <turbo-frame id="venues-list" src="{{ path('venue_list') }}">
        <p>{{ 'common.loading'|trans }}</p>
    </turbo-frame>
{% endblock %}
