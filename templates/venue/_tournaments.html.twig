<turbo-frame id="venue-tournaments">
    <div class="card card-wide">
        <h2>{{ 'nav.tournaments'|trans }}</h2>
        <table aria-label="{{ 'nav.tournaments'|trans }}">
            <thead>
                <tr>
                    <th>{{ 'common.tournament'|trans }}</th>
                    <th></th>
                    <th></th>
                    <th>{{ 'common.date'|trans }}</th>
                    <th>{{ 'common.teams_count'|trans }}</th>
                </tr>
            </thead>
            <tbody>
                {% for t in tournaments %}
                    <tr class="{{ loop.index is odd ? 'row-light' : 'row-dark' }}">
                        <td><a href="{{ path('tournament_session_show', {tournamentId: t.tournamentId, id: t.sessionId}) }}" data-turbo-frame="_top" data-turbo="false">{{ t.tournamentName }}</a></td>
                        <td>{{ include('_format_badge.html.twig', {format: t.tournamentFormat}, with_context: false) }}</td>
                        <td>{{ include('_online_mode_badge.html.twig', {onlineMode: t.tournamentOnlineMode}, with_context: false) }}</td>
                        <td>{{ t.playedAt ? t.playedAt|date('d.m.Y') : '—' }}</td>
                        <td>{{ t.teamsCount }}</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>

        {{ include('_pagination.html.twig', {paginationRoute: 'venue_tournaments', paginationParams: {id: venueId}}) }}
    </div>
</turbo-frame>
